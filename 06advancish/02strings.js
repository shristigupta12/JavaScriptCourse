// `` are better to use instead of quotes because we can use " and ' between the string and values using $

// Strings are inbuilt like array in js so all array methods are applicated
var fullname = `Shristi Gupta`;
console.log(fullname.charAt(2));
console.log(fullname.endsWith("t"));
console.log(fullname.includes("Shris"));
console.log(fullname.toUpperCase());